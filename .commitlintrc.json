{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "scope-enum": [
      2,
      "always",
      ["sensor", "watch", "arm", "status", "install", "terminals", "lint", "test", "ci", "docs", "deps"]
    ],
    "body-max-line-length": [1, "always", 100]
  }
}
